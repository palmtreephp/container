parameters:
  bool: true
  one: 1
  foo: "%env(FOO)%"
  env(FOO): "%bar%"
  bar: 'zorb'
  multi_one: '/path/to'
  multi_two: 'some/file'
  multi: '%multi_one%/%multi_two%'
  escaped_percent: '%%Hello%%'

imports:
  - { resource: 'parameters2.yml' }
